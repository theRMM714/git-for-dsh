# 项目规则

## 核心约束
- 项目始终为GREEN FIELD，没有任何历史包袱
- 项目不存在版本更替，进行任何修改也始终为唯一版本，不要擅自确定版本号等。
- 本仓库是唯一存在的开发仓库，永远不用考虑其它开发者的兼容性。不要为旧版本兼容性，已经不存在的旧实现生产废代码，无意义文档描述
- 贯彻项目理念永远高于兼容性
- 有问题和困惑不要隐瞒或独自推理，直接询问用户
- 用户要求和本规则冲突时警告用户获得肯定答复后再执行
- 复杂任务请灵活运用subagent功能
- 每次执行任务前先总结整体实施方案，等用户审查并同意执行后才开始执行
- 一切文档仅描述当前状态，不对已经迭代的版本进行任何描述
- 完成代码和本地测试后进行本地git提交，git推送只在需要真机测试或actions测试时。
- 任何提交前检查.gitignore是否正确配置，使用git status检查是否有意外被跟踪的文件

## 代码规范
- 必须任意环节、任意实现都可单独 mock 测试（能不能独立测是设计是否合格的一部分；测试放哪由可见性决定，见 [TESTING.md](TESTING.md)）
- 整体必须遵循DIP（依赖倒置）原则
- 注释仅描述当前代码功能和联动模块
- 项目为跨平台项目，所以任何实现先要想能否支持跨平台

## 路径规范（保证项目跨平台/跨目录可迁移）
- 代码与文档中的项目内路径一律使用**相对路径**，以产品根（Cargo.toml 所在目录）为基准，禁止出现 `/mnt/…`、`/home/…`、`C:\…` 等机器相关绝对路径
- 尽量不使用本项目外的目录安装依赖，配置文件等，永远先想办法把一切环境收敛到本项目目录中。
- 代码拼接路径用路径组件（如 `root.join(".home").join("providers.yaml")`），禁止把 `/` 或 `\\` 写进字符串再拼（分隔符交给运行环境）
- 引用仓库内文件（文档、源码、模块清单）同样用相对形式：`README.md`、`modules/<id>/module.yaml`
- 例外（不算绝对路径违规）：①`.gitignore` 的行首 `/` 与 `**/` 是 git 语义；②URL 拼接（如 `{base_url}/chat/completions`）遵循 URL 规则，不是文件路径；③用户私有区（如 `.home/`、模块 `userdata/`）整体搬迁仍以产品根为锚，仅通过环境变量改写根，不引入机器路径

## BUG修复规则
- 低级bug，代码写错了，可以直接修改。
- bug的原因是架构有问题，设计有问题等非低级错误，必须将情况告诉用户，由用户决定如何修改。